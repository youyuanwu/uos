/// A region of DMA-coherent memory.
///
/// Re-exported from `embclox-dma` for backward compatibility.
pub use embclox_dma::DmaRegion;

/// Allocator for DMA-coherent memory regions.
///
/// Re-exported from `embclox-dma` for backward compatibility.
pub use embclox_dma::DmaAllocator;
